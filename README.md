# FinBot — Личный финансовый помощник

## Структура проекта
Основной код находится в папке `bot/`.

## Тесты
Запуск тестов:
```bash
pytest test/

## 🚀 Запуск

1. Создайте `.env` файл на основе `.env.example` и вставьте туда токен.
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt

## 🧩 Технологии
- Python 3.9+
- aiogram 3.10
- SQLite (встроенная БД)

## 🚀 Запуск
1. Скопируйте `.env.example` → `.env` и вставьте туда токен.
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt

## 🗃️ База данных

- Используется `SQLite` через `SQLAlchemy 2.0`.
- Работа с БД — через `Repository`.

## 🗃️ База данных

- Используется `SQLite` через `SQLAlchemy 2.0`.
- Работа с БД — через `Repository`:
  - `UserRepository`: получение/создание/обновление пользователей.
  - `TransactionRepository`: добавление/получение/статистика транзакций.

## 🧪 Тестирование

Для запуска тестов:

```bash
python -m pytest test/ -v

## 💰 Финансы

- `/income` — добавить доход.
- `/expense` — добавить расход (в разработке).
- `/report` — отчеты (в разработке).

- `/income` — добавить доход. Поддерживает отмену и пропуск описания.

## 🧠 Сервисный слой

- `FinanceService` — обрабатывает добавление доходов и расходов.
- Вынесен из обработчиков для лучшей тестируемости и поддержки.

- Все даты и время хранятся в московском часовом поясе (MSK, UTC+3).

## 💰 Финансы

- `/income` — добавить доход.
- `/expense` — добавить расход.
- `/report` — отчёты (в разработке).

- `/report` или кнопка **«📊 Отчёты»** — показывает финансовый отчёт за период (день, неделя, месяц, год).

- В отчётах можно нажать **«📤 Экспорт в Excel»** — получить файл со всеми операциями за период.

- **💳 Долги** — добавление, просмотр активных долгов.